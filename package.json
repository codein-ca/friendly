{
  "name": "@codein/monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "fe": "cd ./packages/fe-app/ && yarn start",
    "sb": "cd ./packages/fe-app/ && yarn storybook"
  }
}
